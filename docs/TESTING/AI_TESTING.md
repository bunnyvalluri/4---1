# 🤖 AI & NLP Testing

Validates the accuracy of resume parsing and AI Assistant behaviors:
- **PyMuPDF Extraction**: Verifies text extraction from generated mock PDF streams.
- **ATS Verb Detection**: Confirms weak verbs are flagged and replaced with XYZ formatted alternatives.
- **Assistant Fallback**: Verifies deterministic domain responses when Gemini API keys are unconfigured.
